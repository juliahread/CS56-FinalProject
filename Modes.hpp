#pragma once

namespace game_modes {
    enum Enum {
        MENU,
        CONTROLS,
        GAMEPLAY,
        ENDGAME
    };
}

namespace menu_modes {
    enum Enum {
        PLAY,
        CONTROLS
    };
}
